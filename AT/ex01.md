cat /proc/net/dev
cat /proc/net/fib_trie | head -30
cat /etc/hosts

Saídas principais:

Interfaces ativas: lo, eth0 (com tráfego), dummy0, sit0.
IPv4: Endereço 172.16.0.2/24 configurado em eth0.
IPv6: Apenas link-local e loopback (não há endereços globais visíveis de forma ativa).

Objetivo da verificação:
Confirmar que o stack IP está carregado, interfaces possuem endereços válidos e o kernel reconhece rotas locais.
Critério: Passou
O host possui conectividade IPv4 ativa (eth0 com IP privado válido e tráfego registrado). IPv6 está habilitado no kernel (presença de ::1 e interfaces), mas sem endereçamento global visível — comum em ambientes de container/VM sem configuração explícita.

cat /proc/net/route

cat /etc/resolv.conf
cat /etc/hosts

hostname
hostname -f
cat /etc/hosts

cat /proc/net/tcp | head -15

Várias entradas em estado 0A (LISTEN) no loopback (0100007F:*), incluindo porta 4240 (1092 hex) e outras conexões locais.
Objetivo da verificação:
Identificar serviços TCP que estão escutando conexões de entrada.
Critério: Passou
Existem sockets TCP no estado LISTEN (principalmente no loopback). Isso indica que o sistema possui serviços ou sockets de aplicação ativos. (Nota: Sem ss/netstat, usamos /proc/net/tcp diretamente — método válido e de baixo nível.)